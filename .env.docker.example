# Mission Control — Docker Environment Variables
# Copy to .env and fill in values:
#   cp .env.docker.example .env

# Jakša's instance
JAKSA_GATEWAY_TOKEN=       # From ~/.openclaw-jaksa/.env → OPENCLAW_GATEWAY_TOKEN
JAKSA_MC_API_TOKEN=        # Generate: openssl rand -hex 32

# Aleksa's instance
ALEKSA_GATEWAY_TOKEN=      # From ~/.openclaw-aleksa/.env → OPENCLAW_GATEWAY_TOKEN
ALEKSA_MC_API_TOKEN=       # Generate: openssl rand -hex 32
